import { createTheme } from "@mui/material";

export const defaultTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#358C6C'
    }
  },
  typography: {
    fontFamily: 'Poppins'
  }
})